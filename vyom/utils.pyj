# from __python__ import dict_literals, overload_getitem
# -*- coding: utf-8 -*-

def init(fn):
    jQuery(main)
    # document.addEventListener("DOMContentLoaded", fn)  # doesn't work


def debug(msg):
    console.log(msg)


def clone(obj):
    o = {}
    for key in obj:
        if obj.hasOwnProperty:
            if not obj.hasOwnProperty(key):
                continue
        o[key] = obj[key]
    return o